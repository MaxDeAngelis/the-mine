using System;

public enum JOB_TYPE { Move, Dig, Chop, Build };

public enum BUILD_SUB_TYPE { None, Wall, Door };

public enum RESOURCE_TYPE { Wood, Stone };

public enum UNIT_STATE {Idle, Busy, Working, Moving};

